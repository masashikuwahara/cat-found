<h1>トップページ</h1>
<table>
  <tr>
    <th>ID</th>
    <th>名前</th>
    <th>場所</th>
  </tr>
  <% @cats.each do |cat| %>
  <tr>
    <td><%= cat.id %></td>
    <td><%= cat.name %></td>
    <td><%= cat.place %></td>
    <td><%= link_to '詳細', cat_path(cat) %></td>
  </tr>
  <% end %>
</table>